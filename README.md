echo "# location-de-voiture"  

git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/steeven-js/location-de-voiture.git
git push -u origin main

